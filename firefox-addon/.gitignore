# Dev-only credentials — never commit
dev-credentials.json

# AMO API-Zugangsdaten für sign-lite.sh — never commit
.amo-credentials

# Build-Artefakte
build/
dist/

# Signierte .xpi-Releases (werden auf den Server hochgeladen, nicht committed)
releases/*.xpi
